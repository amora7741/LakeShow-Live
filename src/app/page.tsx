import ShaderBackground from '@/components/ShaderBackground';
import { Search } from '@/components/Search';

const Home = () => {
  return (
    <ShaderBackground>
      <Search />
    </ShaderBackground>
  );
};

export default Home;
